return {
	"folke/noice.nvim",
	opts = {
		messages = { enabled = false }, -- No command messages
		cmdline = { enabled = false }, -- No cmd UI
		popupmenu = { enabled = false }, -- No popup menu
		notify = { enabled = true }, -- Only enable notifications
	},
}
